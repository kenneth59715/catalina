head	1.2;
access;
symbols;
locks; strict;
comment	@# @;


1.2
date	2007.01.24.22.44.00;	author mmargo;	state Exp;
branches;
next	1.1;

1.1
date	2005.02.14.21.33.42;	author kenneth;	state Exp;
branches;
next	;


desc
@script to print out DATESTAMP.CHECKSUM release info
@


1.2
log
@rename CHECKSUM with VERSIONHASH
mmargo
@
text
@#!___PYTHON_PATH_PLACEHOLDER___
# script to print release identifier

import sys
import string

releaseinfo_list = ('DATESTAMP',
                    'VERSIONHASH'
                   )

releasestrings_list = []

for releasefile in releaseinfo_list :
    try :
        FO = file(releasefile,'r')
        raw_info = FO.read()
        FO.close()
        releasestrings_list.append(string.strip(raw_info))
    except :
        print "%s could not be opened and read!" % releasefile
        sys.exit(1)

list_length = len(releasestrings_list)

release_string = string.join(releasestrings_list,'.')

print release_string

@


1.1
log
@Initial revision
@
text
@d8 1
a8 1
                    'CHECKSUM'
@
